<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme using Material 3 -->
    <style name="Theme.AgentforceDemo" parent="android:Theme.Material.NoActionBar">
        <!-- Primary brand color (Salesforce Blue - lighter for dark mode) -->
        <item name="android:colorPrimary">@color/salesforce_blue_light</item>
        <item name="android:colorPrimaryDark">@color/salesforce_blue</item>
        <item name="android:colorAccent">@color/salesforce_blue_light</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/background_dark</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/background_dark</item>
    </style>
</resources>
